Si en el Mac donde vayas a ejecutar la transcripcion no tienes ffmpeg disponible en el sistema,
copia aqui un binario llamado:

ffmpeg

El bootstrap de macOS prioriza este binario antes de usar ffmpeg del sistema.
